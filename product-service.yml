server:
  port: 8081

spring:
  application:
    name: product-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka  # Eureka 서버 주소.
  instance:
    prefer-ip-address: true
